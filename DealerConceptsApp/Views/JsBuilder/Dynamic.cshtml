
@{
    Layout = null;
}

    (function () {
        'use strict';

        angular
        .module(APPNAME)
        .factory('@Model.Name', CustomService);

        function CustomService() {
        var svc = this;

        svc.kinds = @Html.RawJson(Model.Kinds as object)

        return svc;

        }

        })();